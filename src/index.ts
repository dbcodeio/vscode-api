export * from './types';

// Re-export commonly used types for convenience
export type {
  ConnectionConfig,
  Driver,
  SavePasswordOption,
  ConnectionRole,
  DBCodeAPI,
  ConnectionOperationResult
} from './types';
